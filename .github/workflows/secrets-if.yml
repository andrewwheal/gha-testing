name: Test getting secrets based on conditions

on:
  workflow_dispatch:  # Allows manual trigger
  push:
    branches:
      - secrets-if

jobs:
  get-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Get Secrets 1
        env:
          SECRET_ENV: ea
          SECRET_CUSTOMER: customer1
          SECRET_CUSTOMER_ENV: format("${{ env.SECRET_CUSTOMER }}_${{ env.SECRET_ENV }}")
        run: |
          echo "Secret for ${SECRET_CUSTOMER} = " ${{ vars[env.SECRET_CUSTOMER_ENV] }}
          echo "Secret for ${SECRET_CUSTOMER} = " ${{ vars[env.SECRET_CUSTOMER] }}
      - name: Get Secrets 2
        env:
          SECRET_ENV: ea
          SECRET_CUSTOMER: customer2
        run: |
          echo "Secret for ${SECRET_CUSTOMER} = " ${{ vars[env.SECRET_CUSTOMER] }}
